# Roo Agent Rules

* Follow all rules defined in `AGENTS.md`.
* `AGENTS.md` is the single source of truth.
* Ask clarifying questions if requirements are ambiguous.
* Do not override project rules with Roo-specific assumptions.
