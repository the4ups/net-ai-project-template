# Kilo Code Agent Rules

* Follow all rules defined in `AGENTS.md`.
* `AGENTS.md` is the single source of truth.
* Use Kilo tools only to assist, not to bypass project rules.
* Ask before making large or breaking changes.
