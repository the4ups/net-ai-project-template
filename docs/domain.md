# Domain Rules

## Core Entities

### SomeEntity

Represents the domain entity.

Properties:

- `EntityId` (string) — unique entity identifier

Relationships:

- One entity has many sources (Sources)